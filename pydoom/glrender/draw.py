"""Per-frame GL draw (milestone H, phases 2-3): NOT YET IMPLEMENTED.

Camera uniform + opaque walls/planes, masked mids, sprite billboards
fed by renderer.py projection math, weapon psprite quad. Stub so the
package layout for later phases exists now.
"""
