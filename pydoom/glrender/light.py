"""GL lighting LUTs (milestone H, phase 2): NOT YET IMPLEMENTED.

COLORMAP bytes + PLAYPAL upload as 32x256 / 256x3 LUT textures;
extralight/fullbright row selection mirrors _init_scalelight. Stub so
the package layout for later phases exists now.
"""
