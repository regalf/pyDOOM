"""Static geometry preprocess (milestone H, phase 1): NOT YET IMPLEMENTED.

gl_preprocess.c analog: once per map, wall quads per seg tier (top/
mid/bottom, DONTPEGTOP/DONTPEGBOTTOM honored), sector plane polygons,
sky tags, per-vertex light-ramp keys from the zlight/scalelight
tables. Stub so the package layout for later phases exists now.
"""
