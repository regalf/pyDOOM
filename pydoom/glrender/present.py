"""Overlay compositing (milestone H, phase 4): NOT YET IMPLEMENTED.

Menu/statusbar/HUD/automap/intermission/melt keep drawing into the
320x200 index framebuffer; this uploads it as a nearest-neighbor quad
over the native-res world plane. Stub so the package layout for later
phases exists now.
"""
