"""GL texture builders (milestone H, phase 1): NOT YET IMPLEMENTED.

Wall/flat/sprite patches upload as GL_R8 palette-index textures
(colormap + PLAYPAL stay LUT textures in the fragment shader, so
vanilla lighting reproduces exactly). Stub so the package layout for
later phases exists now.
"""
