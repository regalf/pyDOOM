numpy
pygame-ce
numba
pytest
# OPL music chip (optional: without it music stays silent, like vanilla
# LinuxDoom without its MUS server). DOSBox synth, GPL-3.0-only.
PyOPL
# GL renderer backend (milestone H, optional: without it the viewer
# auto-falls back to the software raster, like headless CI does).
# Stable PyPI releases (not the 4.x alphas); _accelerate is an
# optional C speed-up, the pure-Python core is all the code needs.
PyOpenGL==3.1.10
PyOpenGL_accelerate==3.1.10
